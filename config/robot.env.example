# Jetson と Raspberry Pi Cat の両方で同じ値にする。
ROS_DOMAIN_ID=42
ROS_DISTRO=humble

# Raspberry Pi CatのROSドライバが購読する速度指令トピック。
CMD_VEL_TOPIC=/cmd_vel

# RealSense を複数接続する場合だけシリアル番号を指定する。空欄なら最初の一台を使う。
REALSENSE_SERIAL=

# 0の間は非ゼロの速度指令を拒否する安全スイッチ。
ALLOW_MOTION=0
